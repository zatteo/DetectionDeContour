public class Launcher
{
    public static void main(String args[])
    {
      Fenetre f = new Fenetre();
      // ImageFiltree s = new ImageFiltree("./lena.gif");

      // ImageFiltree s = new ImageFiltree("./lena.gif");
      //
      // ImageFiltree s_gris = new ImageFiltree(s.filtreGris());
      //
      // ImageFiltree s_sobel = new ImageFiltree(s_gris.filtreSobel());
      //
      // // new Affichage(s_sobel.getImage());
      //
      // ImageFiltree s_seuil = new ImageFiltree(s_sobel.seuil(100));
      //
      // ImageVectorisee svg = new ImageVectorisee(s_seuil.getImage());
      //
      // svg.vectorisation_v2(0.3);
    }
}
